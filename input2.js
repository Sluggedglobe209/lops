const readLine = require(`readline-sync`);

//1
let first = readLine.question("Enter the first name : ");
let second = readLine.question("Enter the second name: ");
console.log(`Hello ${second} , ${first}`);




